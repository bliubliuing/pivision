# pivision 使用操作手册（v6.1）

> 写给完全不懂技术的你：跟着本手册一步一步做，就能让 pivision 帮你看图、认视频、生成图片。所有密钥用「你的_密钥」代替，看到它就要换成你自己的真实密钥。

---

## 目录

1. [这是什么？能做什么？](#1-这是什么能做什么)
2. [两种使用方式选哪种？](#2-两种使用方式选哪种)
3. [准备工作：装环境 + 填配置](#3-准备工作装环境--填配置)
4. [方式一：MCP 方式（给 AI 助手用）](#4-方式一mcp-方式给-ai-助手用)
5. [方式二：HTTP 方式（给普通程序用）](#5-方式二http-方式给普通程序用)
6. [b-gen 批量任务文件怎么写](#6-b-gen-批量任务文件怎么写)
7. [常见问题](#7-常见问题)
8. [附：快速命令速查表](#8-附快速命令速查表)

---

## 1. 这是什么？能做什么？

**pivision 是一个「看图 + 生图」的工具箱。** 你给它一张图片，它能用文字告诉你图里有什么；你给它一句话，它能帮你画出一张图。它不需要你自己会写程序，装好之后就能用。

它一共有 **6 个功能**，一句话看懂：

| 功能 | 一句话说明 |
|---|---|
| **r-pic** | 看图识字：识别一张本地图片，返回图里内容的文字描述 |
| **r-vid** | 看视频识字：解析一个视频直链（网址），返回视频内容的文字描述 |
| **g-pic** | 通用生图：你用一句话描述想要的画面，它画一张图给你 |
| **i-pic** | 信息图生图：专门生成「信息图 / 海报 / 图表」这类排版图片 |
| **p-pic** | 图生图：给它一张参考图 + 一句话指令，它照着参考图画一张新图 |
| **b-gen** | 批量干活：把很多任务写进一个文件，一次性批量执行（支持断点续传） |

所有生成或识别用到的图片，都会自动保存到项目里的 **`art/` 文件夹**。

pivision 有**两种用法**（第 2 节会帮你选）：
- **MCP 方式**：装进 AI 助手（比如 CodeBuddy），你在聊天里吩咐它，它自动调用这些功能。
- **HTTP 方式**：装成一个小网站服务（端口 7002），别的程序 / 脚本 / 浏览器可以直接访问它。

---

## 2. 两种使用方式选哪种？

### MCP 方式（推荐给普通使用者）

**是什么：** 把 pivision 挂到你的 AI 编程助手（如 CodeBuddy）里。以后你在对话里说"帮我把这张图识别一下""帮我画一只猫咪"，AI 助手就会自动调用 pivision 的功能，你不用记任何命令。

**适合谁：** 不想碰命令行、喜欢用聊天方式干活的人。

### HTTP 方式（推荐给会写程序的人）

**是什么：** pivision 变成一个网站服务，别人（或你自己写的程序、脚本）用网址就能调用，比如用 curl 命令、用浏览器打开、写 Python 脚本访问。

**适合谁：** 程序员，想把 pivision 的功能接进自己的程序 / 网页 / 自动化脚本。

> **选择建议：** 你是普通使用者 → 用 MCP 方式（第 4 节）。你要写程序调用 → 用 HTTP 方式（第 5 节）。两种方式可以同时开启，互不影响，共用同一份配置。

---

## 3. 准备工作：装环境 + 填配置

> 本节做一次即可。装好环境、填好配置，后面两种方式都能用。

### 第 1 步：打开终端并进入项目文件夹

**做什么：** 打开终端（命令行窗口），进入 pivision 项目所在目录。
**为什么：** 后面所有命令都要在项目目录里执行，找不到目录就做不了。

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision
```

**预期结果：** 回车后没有任何报错，光标停在新的一行，就说明进入成功了。

### 第 2 步：创建 Python 虚拟环境（一个独立的运行空间）

**做什么：** 用一条命令创建虚拟环境（可以理解成"给这个项目单独搭一个干净的运行房间"，避免和电脑上其他程序冲突）。
**为什么：** 不做这步，后面装依赖可能装错地方或装不进去（这台电脑限制系统级安装）。

```bash
python -m venv .venv
```

**预期结果：** 命令执行完回到提示符，项目目录里出现一个 `.venv` 文件夹（表示运行房间搭好了）。

### 第 3 步：安装项目依赖（需要的软件包）

**做什么：** 把 pivision 需要的所有软件包装进刚才的虚拟环境。
**为什么：** 不装的话，程序缺少零件跑不起来。

```bash
.venv/bin/pip install -e .
```

**预期结果：** 看到一大串安装信息，最后出现 "Successfully installed" 字样，就说明装好了。

### 第 4 步：复制配置模板，生成配置文件

**做什么：** 把示例配置文件 `.env.example` 复制一份并改名为 `.env`（这个文件专门存放你的各种密钥）。
**为什么：** 项目自带的是"空模板"，不能直接用；你要在自己的 `.env` 里填真实密钥。注意：`.env` 里有密钥，**不能发给别人、不能提交到代码仓库**。

```bash
cp .env.example .env
```

**预期结果：** 目录里多出一个 `.env` 文件，不报错。

### 第 5 步：打开 .env 文件，填入真实密钥

**做什么：** 用文本编辑器打开 `.env`（比如记事本、VS Code），把里面的密钥填上。
**为什么：** 不填密钥，对应功能调用时会提示"未配置"，无法工作。pivision 有 5 段配置，各管一个功能。

`.env` 文件里 5 段配置的说明（每段都有一个 `ENABLED` 开关，默认都是 `true` 开启）：

| 配置段前缀 | 管哪个功能 | 需要填的关键项 |
|---|---|---|
| `VISION_` | r-pic 图片识别 | `VISION_API_KEY`（视觉密钥，单个） |
| `VISION_VIDEO_` | r-vid 视频识别 | 可以留空，留空自动借用上面的 VISION_；单独填也可 |
| `GEN_IMAGE_` | g-pic 通用生图 | `GEN_IMAGE_API_KEYS`（密钥，可多把用逗号隔开） |
| `GEN_INFOGRAPH_` | i-pic 信息图 | `GEN_INFOGRAPH_API_KEYS`（同上） |
| `EDIT_IMAGE_` | p-pic 图生图 | `EDIT_IMAGE_API_KEYS`（同上） |

以视觉配置为例，打开 `.env` 后你会看到类似这样的内容，把等号后面填空的地方改成你的真实密钥：

```bash
# ---- r-pic 视觉识别（图片）----
VISION_ENABLED=true
VISION_PROVIDER=dots
VISION_MODEL=dots3-note-prev
VISION_BASE_URL=https://note3-prev-api.askdiandian.com/v1
VISION_API_KEY=你的_密钥          # ← 把"你的_密钥"替换成真实密钥
```

要点：
- **视觉（识别）** 填 `VISION_API_KEY` 这一项；`VISION_VIDEO_` 那段**可以全部留空**，会自动借用 VISION_ 的配置。
- **生图（画图）** 填 `*_API_KEYS` 这一项；想用多把密钥分摊额度，就用英文逗号隔开，如 `密钥1,密钥2`。
- 每段开头的 `ENABLED=true` 表示该功能开启；如果你想**彻底关闭某个功能**，把对应的 `true` 改成 `false` 即可。
- 其他项（`MODEL`、`BASE_URL`、`ADAPTER` 等）**一般不用动**，模板里已配好默认值。

**去哪里申请密钥（免费，注册即可用）：**

| 用途 | 去哪个网站申请 | 操作 | 拿到的密钥填到哪 |
|---|---|---|---|
| 视觉（r-pic 看图、r-vid 看视频） | https://dots.ai/platform | 手机号登录 → 进 **API Keys** 页创建 | `VISION_API_KEY` |
| 生图（g-pic / i-pic / p-pic 画图） | https://platform.sensenova.cn | 注册 + 手机验证 → **控制台 → API Keys** 创建 `sk-` 开头的密钥 | `GEN_IMAGE_API_KEYS` / `GEN_INFOGRAPH_API_KEYS` / `EDIT_IMAGE_API_KEYS` |

> 目前这两个平台的 API **都是免费的，额度非常充足**，个人用和大型项目都够用。pivision 等于免费给你的 AI 助手装上了「眼睛」（看图、看视频）和「画笔」（画图）。
> 小提示：如果你已经有其他兼容 OpenAI 的密钥（比如 OpenAI、DeepSeek 等），也可以不改网站、直接把 `*_BASE_URL` 指到自己的地址复用，不必新注册。

**预期结果：** 保存文件后，至少把你需要用到的功能对应的密钥填好。不用的功能先不填也没关系，用到哪个填哪个。

### 第 6 步：验证配置是否就绪（可选）

**做什么：** 用一条命令查看 5 条流水线的配置状态，确认哪些功能已可用。
**为什么：** 提前看清哪些功能"缺钥匙"，省得后面调用时才报错。

```bash
.venv/bin/python -c "from adapters import get_config_summary; [print(r['tool'], r['status']) for r in get_config_summary()]"
```

**预期结果：** 每行显示一个功能名和它的状态。`AVAILABLE` 表示可用；`UNAVAILABLE` 表示这项没配密钥，回去补第 5 步。放心，这条命令**只会显示"已配置 / 未配置"，绝不会打印你的密钥**。

> ⚠️ 重要提醒：**改完 `.env` 后，需要重启服务才生效**（关掉再重新启动，方法见第 4 / 5 节）。

---

## 4. 方式一：MCP 方式（给 AI 助手用）

> MCP 可以理解成"AI 助手的万能插座"——插上 pivision 这个插座，AI 助手就拥有了看图、生图的本事。本节以 CodeBuddy 为例。

### 4.1 注册到你的 AI 助手

**做什么：** 把 pivision 登记到你的 AI 客户端里，告诉它"我这里有这个工具可以用"。
**为什么：** 不登记，AI 助手不知道有这个工具，聊天时就没法调用它。

项目里已准备好两个现成配置文件（内容基本一样，用哪个都行）：
- `mcp-register.json`（带 CodeBuddy 标记）
- `pic-config.json`（通用版）

**打开你 AI 客户端的 MCP 配置入口**，找到它管理 MCP 服务器的配置文件（各家软件位置不同，一般在"设置 / MCP 服务器"里），把 `pic-config.json` 里的内容合并进去。核心内容长这样：

```json
{
  "mcpServers": {
    "pivision": {
      "command": "/home/my/work/0proj/mcp-bulid/zijian/pivision/.venv/bin/python",
      "args": ["/home/my/work/0proj/mcp-bulid/zijian/pivision/pivision.py"]
    }
  }
}
```

- `command` 一栏填项目里的 `.venv/bin/python`（用虚拟环境里的 Python 启动）
- `args` 里的路径指向 `pivision.py`（程序入口）

> ⚠️ **换电脑必读（移植提醒）**：上面 json 里的 `/home/my/work/0proj/mcp-bulid/zijian/pivision/` 是**制作这台电脑的绝对路径**。把项目复制到其他电脑后，`command` 和 `args` 里的路径**必须改成新电脑上项目的实际位置**（比如你解压到 `D:\project\pivision`，就全改成 `D:\project\pivision\.venv\bin\python` 和 `D:\project\pivision\pivision.py`），否则 AI 客户端找不到服务。项目代码本身（pivision.py / utils.py / api_server.py）用的是相对路径，**不需要改**——只有这两个注册配置需要按机器改。

**预期结果：** 保存配置后，AI 客户端里能看到一个叫 `pivision` 的 MCP 服务器，展开能看到 6 个工具：`r-pic`、`r-vid`、`g-pic`、`i-pic`、`p-pic`、`b-gen`。

### 4.2 启动 / 验证

**做什么：** 启动 pivision 服务并确认能连上。
**为什么：** MCP 方式是"随叫随到"的：AI 客户端需要时会自动拉起服务。手动启动一次可以确认环境没问题。

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision
.venv/bin/python pivision.py
```

**预期结果：** 程序不会打印花哨的东西，只是静静地运行（服务在等待 AI 客户端连接）。看到它没报错、光标停在原地，就是正常的。测试完按 `Ctrl + C` 可以停掉；平时交给 AI 客户端自动启动即可。

### 4.3 6 个工具怎么用

MCP 方式下你**不需要敲命令**，直接在 AI 助手的聊天框里用大白话吩咐它。下面是每个功能的"吩咐示例"和它会返回什么。

#### ① r-pic —— 识别本地图片

**用法：** 给 AI 助手一张本地图片的路径，让它识别。
**示例：**

> 用 r-pic 识别这张图：`/你的/图片/photo.png`，告诉我图里有什么。

**参数说明（平时不用管，AI 会自动填）：**

| 参数 | 含义 | 必填 |
|---|---|---|
| `image_path` | 本地图片路径（支持 PNG / JPG / WEBP / GIF） | 必填 |
| `question` | 想让它回答的问题（不填默认"请详细描述这张图片"） | 可选 |
| `max_tokens` | 回复最多多少个字（默认 1024） | 可选 |

**预期返回：** 以 `✅ 识别完成` 开头，下面列出图片文件名、你的提问，以及识别出的文字内容。如果图片有问题，会返回 `❌` 开头的错误说明。

#### ② r-vid —— 识别视频

**用法：** 给 AI 助手一个视频直链（网址），让它解析视频内容。
**示例：**

> 用 r-vid 解析这个视频：`https://你的视频直链.mp4`，总结一下内容。

**参数说明：**

| 参数 | 含义 | 必填 |
|---|---|---|
| `video_url` | 视频直链（http/https 开头，**必须公网能直接访问**，本地地址不行） | 必填 |
| `question` | 想让它回答的问题（默认"请详细描述这段视频"） | 可选 |
| `max_tokens` | 回复上限（默认 8192，视频内容长，建议别调小） | 可选 |

**预期返回：** 以 `✅ 视频解析完成` 开头，附视频链接、提问和解析结果。视频解析比较慢，可能要等一会儿。

#### ③ g-pic —— 通用生图（文生图）

**用法：** 用一句话描述想要的画面，让它画出来。
**示例：**

> 用 g-pic 画一张：一只猫咪坐在咖啡厅窗边，阳光洒进来，插画风格。

**参数说明：**

| 参数 | 含义 | 必填 |
|---|---|---|
| `prompt` | 画面描述（越具体越好） | 必填 |
| `size` | 图片尺寸，如 `2752x1536`（默认）、`1024x1024` | 可选 |
| `n` | 生成几张（1~20，默认 1；多于 1 张会逐张生成） | 可选 |
| `provider` | 生图后端（默认读配置，一般不用填） | 可选 |
| `instance` | 命名实例名（高级功能，没配置就用不到） | 可选 |

**预期返回：** 以 `✅ 图片已生成` 开头，给出文件名、本地保存路径（在 `art/` 下）和一个临时的图片 URL。

#### ④ i-pic —— 信息图生图

**用法：** 和 g-pic 一样，但专门用于生成信息图 / 海报 / 图表类排版。
**示例：**

> 用 i-pic 做一张信息图：介绍"每天喝水的 8 个好处"，配数字和图标。

**参数说明：** 同 g-pic（`prompt` / `size` / `n` / `provider` / `instance`），只是走的模型更适合排版类图片。

**预期返回：** 以 `✅ 图片已生成` 开头，输出文件名、本地路径和临时 URL。

#### ⑤ p-pic —— 图生图（参考图 + 指令）

**用法：** 给一张参考图 + 一句修改指令，让它基于参考图画新图。
**示例：**

> 用 p-pic，参考图是 `/你的/图片/sketch.png`，指令是"把背景改成雪山"。

**参数说明：**

| 参数 | 含义 | 必填 |
|---|---|---|
| `prompt` | 编辑指令，描述你想要的最终画面 | 必填 |
| `image_path` | 参考图的本地路径 | 必填 |
| `size` | 图片尺寸 | 可选 |

**预期返回：** 以 `✅ 图生图完成` 开头，给出文件名、本地路径和临时 URL。注意：p-pic 一次只生成 1 张（不支持 n）。

#### ⑥ b-gen —— 批量引擎

**用法：** 把很多任务写进一个 `.txt` / `.md` 文件，一次批量执行（每行一个任务），支持断点续传。
**示例：**

> 用 b-gen，task 是 g-pic，从 `/你的/任务文件/prompts.txt` 批量生成图片，每两个任务间隔 1 秒，断点续传开启。

**参数说明：**

| 参数 | 含义 | 必填 |
|---|---|---|
| `task` | 任务类型，填 `g-pic` / `i-pic` / `p-pic` / `r-pic` / `r-vid` 之一 | 必填 |
| `file_path` | 任务文件的本地路径 | 必填 |
| `interval` | 相邻任务间隔几秒（默认 0，防止被限流时可设 1~2） | 可选 |
| `resume` | 是否断点续传（填 `true`，中断后重跑会跳过已完成的任务） | 可选 |
| `size` / `n` | 只对生图任务有效 | 可选 |

**预期返回：** 以 `✅ 批量生成完成！共 N 个任务` 开头，逐个列出每个任务的执行结果，并把详细结果保存到项目里的 `pivision_batch_results.txt`（返回里会附上这个文件的完整路径）。文件写法见[第 6 节](#6-b-gen-批量任务文件怎么写)。

### 4.4 图片存在哪里？

所有生成 / 识别用到的图片，都自动保存到项目的 **`art/` 文件夹**：

```bash
/home/my/work/0proj/mcp-bulid/zijian/pivision/art/
```

**预期结果：** 生成图片后，去这个文件夹就能看到图片文件（文件名形如 `描述_日期时间_随机码.png`）。直接用本地路径打开即可，**不要依赖工具返回的临时网址**（网址有时效，见第 7 节）。

---

## 5. 方式二：HTTP 方式（给普通程序用）

> HTTP 方式把 pivision 变成一个小网站服务，任何程序都能通过"网址请求"来调用。看不懂 JSON 没关系，跟着示例复制粘贴即可。

### 5.1 启动服务

**做什么：** 启动 HTTP 服务，让它开始监听 7002 端口（端口可以理解成"这扇门的门牌号"）。
**为什么：** 不启动，外部程序就访问不到它。

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision
source .venv/bin/activate
python api_server.py
```

**预期结果：** 看到类似下面的日志，说明启动成功（服务会一直挂着，不要关这个窗口；测试完按 `Ctrl + C` 可停止）：

```text
INFO:     Uvicorn running on http://127.0.0.1:7002
INFO:     pivision API 服务器就绪（HTTP 端口 7002）
```

> 启动后**另开一个终端窗口**来执行下面所有测试命令（原窗口保持服务运行）。

### 5.2 后台守护启动（可选，关掉终端也不停）

**做什么：** 用一条命令让 HTTP 服务在后台悄悄运行。这样就算你把启动它的终端窗口关掉，服务也不会停。

**为什么：** 第 5.1 节是前台启动，一关终端窗口服务就停了。如果你想让 pivision 长期挂着（比如随时能被别人 / 别的程序调用），就用后台启动。

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision
source .venv/bin/activate
nohup python api_server.py > pivision_server.log 2>&1 &
```

**预期结果：** 命令回车后**立刻回到提示符**（不会卡住），服务已在后台运行。程序打印的日志会写到项目里的 `pivision_server.log` 文件（想看日志就打开这个文件）。

**怎么确认它在跑：** 打开浏览器访问 `http://127.0.0.1:7002/health`，显示 `{"status": "ok", "service": "pivision"}` 就是活着。也可以在终端里查进程：

```bash
ps aux | grep api_server.py
```

能看到一条包含 `api_server.py` 的记录就说明在跑（最后一行 `grep` 自己，可以忽略）。

**怎么停掉它：**

```bash
pkill -f api_server.py
```

**预期结果：** 执行完没有输出。再用浏览器访问 `http://127.0.0.1:7002/health`，打不开 / 没反应，说明服务已停止。

> ⚠️ 注意：后台启动的服务，改完 `.env` 后也要重启才生效——先 `pkill -f api_server.py` 停掉，再重新执行上面的 nohup 启动命令。

---

### 5.3 统一响应格式（先看懂它）

**做什么：** 认识一下所有接口返回的"标准答案格式"，方便判断成功还是失败。
**为什么：** 每个接口都返回同一格式，你只要看 `code` 一个数字就知道结果。

所有接口统一返回三选一：

| `code` | 含义 | 怎么判断 |
|---|---|---|
| `0` | 成功 | `msg` 是 `"ok"`，真正的结果在 `data` 里 |
| `1` | 业务失败（比如没配密钥、图片不存在） | 看 `msg` 里的错误说明 |
| `2` | 程序异常（出 bug 了） | 看 `msg` 里的异常信息 |

成功示例：

```json
{"code": 0, "msg": "ok", "data": "✅ 图片已生成（后端：sensenova）\n文件名：...\n本地：.../art/xxx.png\nURL：https://..."}
```

### 5.4 8 个接口逐个用

> 下面的 `curl` 命令可以直接复制，把里面的示例路径 / 网址 / 描述替换成你自己的即可。

#### ① GET /health —— 健康检查

**做什么：** 检查服务活着没有。
**为什么：** 服务没启动时，先用它确认。

```bash
curl http://127.0.0.1:7002/health
```

**预期返回：**

```json
{"status": "ok", "service": "pivision"}
```

#### ② GET /tools —— 工具清单

**做什么：** 查看服务提供了哪些功能。

```bash
curl http://127.0.0.1:7002/tools
```

**预期返回：**

```json
{"tools": ["r-pic", "r-vid", "g-pic", "i-pic", "p-pic", "b-gen"]}
```

#### ③ POST /r-pic —— 图片识别

**做什么：** 识别一张本地图片。

```bash
curl -X POST http://127.0.0.1:7002/r-pic \
  -H "Content-Type: application/json" \
  -d '{"image_path": "/你的/图片/photo.png", "question": "图片里有什么动物？"}'
```

**请求字段：** `image_path`（必填）、`question`（可选）、`max_tokens`（可选）。

#### ④ POST /r-vid —— 视频识别

**做什么：** 解析一个公网视频直链。

```bash
curl -X POST http://127.0.0.1:7002/r-vid \
  -H "Content-Type: application/json" \
  -d '{"video_url": "https://你的视频直链.mp4", "question": "总结视频内容"}'
```

**请求字段：** `video_url`（必填）、`question`（可选）、`max_tokens`（可选）。视频地址必须是公网能直接访问的 http/https 链接。

#### ⑤ POST /g-pic —— 通用生图

**做什么：** 根据一句话生成图片。

```bash
curl -X POST http://127.0.0.1:7002/g-pic \
  -H "Content-Type: application/json" \
  -d '{"prompt": "一只猫咪坐在咖啡厅窗边，插画风格", "size": "2752x1536", "n": 1}'
```

**请求字段：** `prompt`（必填）、`size`（可选，默认 `2752x1536`）、`n`（可选，1~20）、`instance`（可选）。

#### ⑥ POST /i-pic —— 信息图生图

**做什么：** 生成信息图 / 海报类图片。

```bash
curl -X POST http://127.0.0.1:7002/i-pic \
  -H "Content-Type: application/json" \
  -d '{"prompt": "每天喝水的 8 个好处信息图"}'
```

**请求字段：** 同 g-pic（`prompt` / `size` / `n` / `instance`）。

#### ⑦ POST /p-pic —— 图生图

**做什么：** 参考一张图 + 指令生成新图。

```bash
curl -X POST http://127.0.0.1:7002/p-pic \
  -H "Content-Type: application/json" \
  -d '{"prompt": "把背景改成雪山", "image_path": "/你的/图片/sketch.png"}'
```

**请求字段：** `prompt`（必填）、`image_path`（必填）、`size`（可选）、`instance`（可选）。一次生成 1 张。

#### ⑧ POST /b-gen —— 批量引擎

**做什么：** 从任务文件批量执行。

```bash
curl -X POST http://127.0.0.1:7002/b-gen \
  -H "Content-Type: application/json" \
  -d '{"task": "g-pic", "file_path": "/你的/任务文件/prompts.txt", "interval": 1, "resume": true}'
```

**请求字段：** `task`（必填）、`file_path`（必填）、`interval`（可选）、`resume`（可选）、`size` / `n`（可选，仅生图任务有效）。文件格式见[第 6 节](#6-b-gen-批量任务文件怎么写)。

### 5.5 用浏览器也能测（可选）

**做什么：** 不开终端也能确认服务活着。
**为什么：** 图省事，方便快速检查。

直接在浏览器地址栏打开：

```text
http://127.0.0.1:7002/health
```

**预期结果：** 浏览器里显示 `{"status": "ok", "service": "pivision"}`，说明服务正常运行。

---

## 6. b-gen 批量任务文件怎么写

### 6.1 任务文件格式

**做什么：** 建一个 `.txt` 或 `.md` 文件，**每行一个任务**。
**为什么：** b-gen 按"行"读任务，一行就是一个任务，格式错了会跳过或报错。

不同 `task` 的每行写法不一样：

| `task` | 每行写什么 | 示例行 |
|---|---|---|
| `g-pic` / `i-pic` | 每行一个画面描述（prompt） | `一只猫咪咖啡厅插图` |
| `r-pic` | 每行一个图片路径 | `/你的/图片/photo.png` |
| `r-vid` | 每行一个视频直链 | `https://你的视频直链.mp4` |
| `p-pic` | 每行：`描述 | 参考图路径`（中间用竖线 `|` 隔开） | `把背景改成雪山 | /你的/图片/sketch.png` |

**任务文件示例（g-pic 用）：**

```text
一只猫咪咖啡厅插图
一张秋季森林风景画
一杯冒热气的拿铁，暖色调
```

### 6.2 断点续传怎么用

**做什么：** 批量任务很多、怕中途断了白做时，开启断点续传。
**为什么：** 不开的话，中断后重跑会从第一行全部重来一遍，浪费时间和额度。

**用法：** 调用 b-gen 时把 `resume` 设为 `true`：

- 执行过程中，进度会记录在项目里的 `.bgen_progress.json` 文件中（自动管理，不用管它）。
- 中途断掉后，重新用**相同的 task 和相同的文件**调用 b-gen（`resume` 仍为 `true`），会**自动跳过已完成的行**，只做没做完的。
- 如果任务类型或输入文件变了，旧进度会自动作废、从头开始，不会串错。

### 6.3 结果在哪看

**做什么：** 查看批量执行的结果。
**为什么：** 结果太长，直接写在聊天/响应里不方便，所以单独存成文件。

**预期结果：** 批量执行后，项目根目录会生成 `pivision_batch_results.txt`，里面有每个任务的执行情况；b-gen 的返回信息里也会附上这个文件的完整路径。打开看即可。

---

## 7. 常见问题

**Q1：生成的图片去哪了？**
全部自动保存到项目的 `art/` 文件夹（`pivision/art/`）。工具返回的图片网址只是临时链接，**看图片请用本地文件路径**。

**Q2：返回的图片网址为什么打不开 / 过期了？**
生图服务返回的网址有时效（一般 1 小时 ~ 24 小时就失效）。pivision 在生成时会**自动把图片下载保存到本地 `art/`**，所以不用管那个网址，直接用本地文件。

**Q3：视频识别报错 / 识别不了？**
视频直链**必须是公网能直接访问的 http/https 链接**，局域网地址、`localhost`、本机文件路径都不行。视频解析较慢，属正常现象，耐心等一会儿；如果超时，把视频换成更短/更小的再试。

**Q4：调用生图返回"未配置 xxx_API_KEYS"？**
对应的功能没填密钥。打开 `.env`，把对应段落的密钥填上（生图填 `*_API_KEYS`，可多把逗号分隔），**然后重启服务**。

**Q5：返回"该流水线已停用（xxx_ENABLED=false）"？**
对应功能的开关被关掉了。打开 `.env`，把对应的 `ENABLED` 改成 `true`（或删掉这行 / 留空都算开启），然后重启服务。

**Q6：我改了 .env 配置，为什么不生效？**
服务是在**启动时**读取一次配置的。改完 `.env` 必须**重启服务**（停掉再启动，见第 4.2 / 5.1 / 5.2 节）才会生效。

**Q7：想多把密钥分担额度，怎么配？**
生图密钥支持多把，在 `.env` 的 `*_API_KEYS` 里用英文逗号隔开即可，如 `密钥1,密钥2`。多把密钥必须指向**同一个服务、同一个模型**，不要混不同厂家的密钥。

**Q8：某行任务失败会中断整批吗？**
不会。b-gen 逐行执行，单行失败只影响那一行，其余任务继续跑，结果里会标注失败原因。

---

## 8. 附：快速命令速查表

**环境安装（首次）**

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision
python -m venv .venv
.venv/bin/pip install -e .
cp .env.example .env
```

**配置检查**

```bash
.venv/bin/python -c "from adapters import get_config_summary; [print(r['tool'], r['status']) for r in get_config_summary()]"
```

**启动 MCP 服务（方式一）**

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision && .venv/bin/python pivision.py
```

**启动 HTTP 服务（方式二）**

```bash
cd /home/my/work/0proj/mcp-bulid/zijian/pivision && source .venv/bin/activate && python api_server.py          # 前台（关窗口即停）
nohup python api_server.py > pivision_server.log 2>&1 &   # 后台（关窗口不停）；停掉用：pkill -f api_server.py
```

**HTTP 接口速查**

```bash
curl http://127.0.0.1:7002/health            # 健康检查
curl http://127.0.0.1:7002/tools             # 工具清单
curl -X POST http://127.0.0.1:7002/r-pic -H "Content-Type: application/json" -d '{"image_path": "/你的/图片/photo.png"}'
curl -X POST http://127.0.0.1:7002/r-vid -H "Content-Type: application/json" -d '{"video_url": "https://你的视频直链.mp4"}'
curl -X POST http://127.0.0.1:7002/g-pic -H "Content-Type: application/json" -d '{"prompt": "一只猫咪插图"}'
curl -X POST http://127.0.0.1:7002/i-pic -H "Content-Type: application/json" -d '{"prompt": "喝水好处信息图"}'
curl -X POST http://127.0.0.1:7002/p-pic -H "Content-Type: application/json" -d '{"prompt": "背景改成雪山", "image_path": "/你的/图片/sketch.png"}'
curl -X POST http://127.0.0.1:7002/b-gen -H "Content-Type: application/json" -d '{"task": "g-pic", "file_path": "/你的/任务文件/prompts.txt"}'
```

**常用文件位置**

| 内容 | 位置 |
|---|---|
| 生成 / 识别的图片 | 项目 `art/` 文件夹 |
| 批量任务进度（断点续传用） | 项目 `.bgen_progress.json`（自动管理） |
| 批量执行结果 | 项目 `pivision_batch_results.txt` |
| 密钥配置 | 项目 `.env`（勿外传） |
